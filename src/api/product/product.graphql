enum ProductType {
  bag
  shirt
  hoody
  bottoms
  shoes
}

type Product {
  id: ID!
  name: String!
  price: Float!
  quantity: Int!
  type: ProductType!
}

type Query {
  productDetail(id: ID!): Product!
}
